# サイトの現在の状態（STATE.md）

このファイルは、サイトの現在の状況やできていること、次にやることを記録するためのドキュメントです。

最終更新：2026-09-19（Claude：レビュー本文10本・訪問時期表示）

## レビュー本文10本の追加と訪問時期の表示（2026-09-19・Claude）

- 点数カードだけだったレビューのうち10本（TOTONOI、サ郷kakeRu、音戸温泉、キャナルリゾート、京王高尾山温泉 極楽湯、湯らっくす、スカイスパ、佐賀大和温泉ホテルアマンディ、スパワールド、東京染井温泉 SAKURA）に「◎良い点／△惜しい点／本文」を追加した。
- 材料はGOMIRACHELIN_v13（ブログの正本シート、ID 1VQE0…）の「メモ(体験)」とサ活記録を主に、「参考情報：メリット／デメリット」を補助に使用。体験でない参考情報は「私が感じた」とは書かず、一般的な情報として記述した。
- 文体は既存記事に合わせ、本文3段落（施設紹介と良い点 →「ただ、正直に申し上げると、」→「それでも、」で締める）、良い点・惜しい点は短い体言止め、本文は約240〜300字。
- 点数カード12本の見出し行に、v13の「訪問時期」を追加（既存記事と同じ「エリア ／ 訪問 YYYY年M月 ／ 平日¥N」形式）。訪問時期が空欄のアマンディは訪問表記なし。
- 点数（総合・コスパ・10項目）は `data/rankings.json` のままで、v13と全176施設一致を確認済み。
- ふくの湯・上野ステーションホステル オリエンタル3は体験メモが無い／短いため本文未作成（点数カードのみ）。オリエンタル3は参考情報に「2022年3月22日 改装のため閉店」とあり営業状況の確認が必要。
- 判断と理由：旧シート（ID 1G8Y…、⑩立地）はブログに使っていないため、サイトの点数はv13を正とする。

## サウナ大学の次レッスン導線・空レビューの点数表示・地図の計測（2026-09-18・Claude）

- **サウナ大学「次の未完了レッスンへ」**：章を修了しても「カリキュラムにもどる」しか導線がなかった問題を解消。`sauna-daigaku.html` に `orderedChapters()` `firstOpenLesson()` `nextIncomplete()` `chapterNextCTA()` とCSS `.nextcta` を追加した。章詳細のレッスン一覧の下に「つづきから／次の未完了レッスン ・ ◯◯章」ボタンを出し、レッスン下部の完了ボタンは章の最終レッスンで「このレッスンを完了 ・ 次の章へ →」になり、押すと次の未完了レッスンへ直行する（章が変わるときはトーストで知らせる）。段位ロック（前の段80%）と前レッスン未完了ロックはこれまでどおり。全レッスン修了時は「カリキュラムへ」ボタン。
- この改修はもともとConoHa本番にだけ入っていた。ConoHa解約で失われないようGitHubの正本へ取り込んだ。判断と理由：本番にしか無い変更はGitHubへ先に取り込む。移行後に作業が消えるのを防ぐため。
- **空だったレビュー記事12本に点数カードを追加**：`data/rankings.json` の総合点・コスパ値・料金・10項目スコアだけを使い、完成記事と同じマークアップの点数カード（OVERALL／COST PERF／SCORE BREAKDOWN）を挿入した。対象は totonoi、サ郷kakeRu、ふくの湯、音戸温泉、スパワールド、東京染井温泉 SAKURA、キャナルリゾート、京王高尾山温泉 極楽湯、サウナと天然温泉 湯らっくす、上野ステーションホステル オリエンタル3、スカイスパ、佐賀大和温泉ホテルアマンディ。
- 良い点・惜しい点・レビュー本文は**書いていない**。運営者の体験が必要な部分なので、あとから書き足す前提。見出し行は完成記事の「エリア ／ 訪問YYYY年M月 ／ 平日¥N」に対し、訪問年月が正本に無いため「エリア ／ 平日¥N」とした。判断と理由：正本に無い数字や体験は作らない。
- **サウナ地図に計測と正規URLを追加**：`map/index.html` にGA4（G-GTF2GM6DNJ）と canonical を追加。`sitemap.xml` に `/map/` を追加し、61→62URLになった。判断と理由：地図が使われているか分からない状態だったため。`scripts/build_map_data.py` はデータ部分のみを置換する作りで、この追加後も施設データ・境界データの検出に成功することを確認済み。
- 確認結果：点数カード12本すべて、10項目・総合・コスパ・料金・バーの幅と色が `data/rankings.json` と一致し、完成記事（テルマー湯）とマークアップ構造が完全一致。既存リンクの欠落ゼロ、divの開閉数の整合を確認。`sauna-daigaku.html` は全scriptの構文検査に合格し、未学習・第1章完了・下段全完了・全完了の4パターンで遷移先が期待どおりであることをサンドボックスで確認。
- 変更ファイル：`sauna-daigaku.html`、`map/index.html`、`sitemap.xml`、上記12施設の `index.html`、この `docs/STATE.md`。
- 公開状況：GitHub Pagesへ反映（この更新で公開）。ConoHa、DNS、GAS、Google Sheetsは変更していない。
- 残作業：12本の「良い点・惜しい点・レビュー本文」を運営者が書く。ConoHa解約前にDNS・メール・ドメイン契約の確認（`docs/dns-mail-公開記録.md` のとおりNSとMXがConoHa配下のため、解約前に移す必要がある）。

## 地図データ正本化・座標補完（2026-09-07・Codex）

- `data/map-facilities.json` を静的地図の正本として追加し、採点が空だった「毎日サウナ東京 幕張店」の重複1行を除いた。画面の掲載数は174施設のまま。
- `scripts/build_map_data.py` で `map/index.html` を生成・同期検査できるようにした。座標が日本域内か、登録都道府県の境界内かも検査する。
- 記事がある座標未登録11施設を、公式住所、Google Maps、OpenStreetMapの一致で確認して追加。座標ありは54件から65件、未登録は120件から109件になった。
- ローカル表示で47都道府県境界、全国174施設、拡大時の65ピン、新規追加した渋谷SAUNASのピン存在を確認した。
- コミット `589b89a` のPages反映後、公開地図でも65ピンと大阪サウナDESSE・渋谷SAUNAS・神戸クアハウスの新規ピンを確認した。
- 座標の出典は `data/map-coordinate-sources.json`、更新手順は `docs/地図データ更新手順.md` に保存。ConoHa、DNS、GAS、Google Sheetsは変更していない。
- 続けて未登録20件を検索し、完全一致した5件（カッタの湯、亜熱帯サウナ、MACHI:SAUNA、ほの湯楽々園、カンデオ広島八丁堀）を追加。座標あり70件、未登録104件。候補なし15件は保留した。
- コミット `c714ac9` のPages反映後、公開地図で70ピンと追加したカッタの湯・亜熱帯サウナ・カンデオ広島八丁堀を確認した。
- 次の未登録20件を調べ、施設名の完全一致、公式住所、公式サイト内のGoogle Maps、国土地理院住所検索で確認できた10件を追加した。座標あり80件、未登録94件。確認できなかった10件は推測せず保留した。
- 追加施設は、雲母の里、NOT BUSY、コロナの湯小倉、sauna kolme kylä、サウナ日本、ゴールデンタイム高松、EMウェルネス、御成桑拿、別府温泉保養ランド、サウナ海。都道府県境界内検査、全HTML内部リンク検査、URL・ランキング同期検査に合格。
- コミット `328e7a2` をmainへpush後、公開地図で位置登録80・未登録94と、追加した10施設すべてのピンを確認した。
- 判断と理由：施設名だけで見つからない場合も、公式住所と公的住所検索が一致したものは登録する。店舗名の誤一致を避けながら補完を速められるため。
- 続く未登録20件では、OpenStreetMapで完全一致した5件と、公式住所を国土地理院住所検索で照合した10件を追加した。座標あり95件、未登録79件。確認できなかった5件は保留した。
- 追加施設は、いつつばしの里、ぬかとゆげ、COCOFUROおおみね湯、ルーマプラザ、関門の湯、湧く和来、ホテル・アンドルームス那覇ポート、空庭温泉、COCOFUROかが浴場、SaunaLab Kanda、COCOFUROますの湯、豊島園庭の湯、ウェルビー今池、天光の湯、湯守座。県境内検査と全サイト検査に合格。
- コミット `c436519` をmainへpush後、公開地図で位置登録95・未登録79と、追加した15施設すべてのピンを確認した。
- 次の20件をOpenStreetMapへ照合する処理は、自動承認審査が「施設名・都道府県・市区町村の具体的な外部送信承認が必要」として一度拒否した。その後、利用者から「つづけて」と明示承認を得て、公開済みの施設名・都道府県・市区町村だけを送信した。点数や非公開データは送信していない。
- この20件からOpenStreetMapの施設名・地域一致7件と、公式住所＋国土地理院住所検索7件の計14件を追加した。座標あり109件、未登録65件。追加施設は、ザ・センチュリオンサウナ札幌、星遊館、北こぶし知床、KIKI知床、白銀荘、お湯むすび、森乃彩、金の亀、オリエンタル赤坂、サウナ横綱、タイムズ スパ・レスタ、南柏すみれ、sauna kukka、とれとれの湯。
- 現在の公式サイトが登録地域と食い違うsaunahouseなど、十分に確認できなかった6件は推測せず保留した。県境内検査、全HTML内部リンク、URL・ランキング同期検査に合格。ConoHa、DNS、GAS、Google Sheetsは変更していない。

## 旧URL履歴の分離（2026-09-07・Codex）

- `data/urls.json` からアスティルとオリエンタル2の旧 `-2` 投稿を外し、現在使う58件だけに整理した。
- 旧2件は、正式URL・整理理由とともに `data/url-history.json` へ保存した。旧URLの転送ページと正式ページは削除していない。
- `scripts/check_url_data.py` を追加し、現在URLと履歴の重複、転送先、両フォルダーの存在を検査できるようにした。
- `scripts/gen_reviews.py` のテンプレートにもサウナ地図メニューを追加し、再生成でメニューが消える問題を防いだ。
- URL検査、ランキング同期、全64 HTMLの内部リンク検査に合格。ConoHa、DNS、GAS、Google Sheetsは変更していない。

## 10項目評価基準の統一（2026-09-07・Codex）

- トップページの「10の物差し」を基準に `docs/10項目評価基準.md` を作成し、⑤ロウリュ、⑦スパ外施設、⑧清潔さの混同しやすい範囲を明文化した。
- ⑤はロウリュ・アウフグースの提供内容と質、⑦は屋外ではなく浴室外の館内施設、⑧は築年数から切り離した清掃・衛生とした。
- `data/rankings.json` の画面説明を統一し、`scripts/build_rankings.py` が項目カード・見出し・説明・順位を同じ正本から生成するよう拡張した。`--check` 成功。
- GAS公開版は変更していない。公開GitHub外のサニタイズ済みGASバックアップも同じ定義へそろえ、JavaScript構文検査に合格した（SHA-256 `F9D9086AA1503A6212D5F9706DF14527E5CE6B3B034206D60D5D63B4162689E9`）。

## オリエンタル2旧URL整理（2026-09-07・Codex）

- `上野ステーションホステル-オリエンタル2-2/` を削除せず、`noindex`、正式URLcanonical、即時移動、手動リンクを持つ案内ページに変更した。
- 正式ページは総合64、コスパ27.5、10項目、レビュー本文、宿泊リンクをローカル表示で確認。説明文に残っていた古い67・26.8も64・27.5へ統一した。
- HTML 64ファイル、内部参照1,467件、存在しない参照0件。ConoHa、DNS、GAS、Google Sheetsは変更していない。

## ランキングデータ一本化（2026-09-06・Codex）

- `data/rankings.json` をランキングデータの正本として追加。通常ランキング175施設と、10項目の点数を持つ48施設を1か所で管理できる。
- `scripts/build_rankings.py` を追加。通常ランキングの施設データと10項目別ランキングの10順位一覧を静的HTMLとして生成する。表示、SEO、JavaScriptの動作方式は維持。
- 現在のHTMLから初期データを作り、再生成後に `--check` が成功。10項目別ページは生成前後で内容差分0。
- 「毎日サウナ東京 幕張店」の同名重複2件から、総合点が空の記録を除き、総合27の記録を残した。正本内の同名重複は0件。
- `docs/ランキング更新手順.md` と `AGENTS.md` に、Codex・Claude・Grokなど全AI共通の更新方法を記録した。
- 全64 HTML、内部参照1,481件、欠落0。ConoHa、DNS、GAS、Google Sheetsは変更していない。

## アスティル重複ページ整理（2026-09-06・Codex）

- ランキングとレビュー一覧が参照する接尾辞なしの正式URLへ、`-2` 側にあった完全なレビュー本文を移した。
- 正式ページのコスパ表記をランキング・地図と同じ21.4に統一。総合60、10項目、本文、外部リンクをローカル表示で確認した。
- 旧 `-2` URLは削除せず、`noindex` と正式URLのcanonicalを付けた移動案内ページにした。自動移動できない場合の手動リンクもある。
- HTML 64ファイル、内部参照1,481件を再検査し、存在しない参照0件。ConoHa、DNS、GAS、Google Sheetsは変更していない。

## Sheets部分バックアップ確認・重複ページ比較（2026-09-06・Codex）

- 公開GitHub外の `gomirachelin-work/map-fix/` に、「サウナDB」179行・最大46列の値スナップショットと地図用派生データがあることを、ファイルサイズとSHA-256を含めて確認した。
- これは地図データの部分バックアップ。数式、書式、保護設定、他シートを含む完全なGoogle Sheetsバックアップは未完了。生データは公開GitHubへ追加しない。
- 詳細と次の確認手順を `docs/GoogleSheetsバックアップ状況.md` に記録した。元のGoogle Sheetsは変更していない。
- 重複ページ2組を比較した。アスティルは `-2` 側に完全な本文があり、オリエンタル2は接尾辞なし側に完全な本文がある。正式URLはどちらも接尾辞なしを維持する方針。移植・削除・リダイレクトはまだ行っていない。

## 公開スマホ確認・全リンク検査・整理候補（2026-09-06・Codex）

- コミット `7b0fdb8` をGitHub mainへ反映し、公開URLの390×844表示でトップMENUが開くことを再確認した。
- 公開URLのお問い合わせページは横はみ出し0、画像エラー0、Googleフォームへのボタンを確認した。
- 存在しないURLで `404.html` の「ページが見つかりません」が表示され、トップ・レビュー一覧へ戻れることを確認した。
- リポジトリ内のHTML 64ファイル、内部参照1,495件を機械検査し、存在しない参照0件だった。
- `docs/フォルダー整理候補.md` を作成。公開URLを守るため施設フォルダーは移動せず、同名で接尾辞付きの2組を内容比較が必要な候補として記録した。削除・移動は0件。

## スマートフォンMENU修正と主要ページ確認（2026-09-06・Codex）

- Chromeのモバイル端末エミュレーション（390×844）で、トップ、ランキング、レビュー一覧、サウナ大学、静的地図を確認した。
- 全5ページで横はみ出し0、読み込み失敗画像0。トップの大見出しも画面内に収まることを確認した。
- 共通MENUボタンを押してもリンク一覧が開かない不具合を発見。`css/site.css` のセレクタを `#nav-check:checked ~ .site-header .nav-links` へ修正した。
- 修正後、トップ、ランキング、レビュー一覧、サウナ大学でMENUが `display:flex` になり、6項目が見えることを確認した。静的地図は専用画面のため左上の「GOMIRACHELINトップへ」を使用する。
- Claude CLIへの読み取りレビューは、自動承認が第三者サービスへのコード送信として拒否したため実施していない。Claudeへコードは送信されていない。

## GitHub PagesをルートURLで正常公開（2026-09-06・Codex）

- リポジトリ名を `gomirachelin` から `kenji0618y.github.io` へ変更した。GitHubの旧リポジトリURLは新URLへ転送される。
- GitHub Pagesの公開URLは `https://kenji0618y.github.io/` になった。トップ、CSS、メニュー、WHY・ランキング等のルート絶対パスが正しい場所を指すため、1,434か所のパス問題は一括解消した。
- トップページと `https://kenji0618y.github.io/map/` を未ログインで開き、デザイン、主要メニュー、全国174施設、東京都47施設、47都道府県の境界を確認した。
- ローカル作業コピー `gomirachelin-work/sync-repo` の `origin` も新リポジトリURLへ変更した。
- 方式Aを選んだ理由：無料ですぐ実施でき、ConoHa、DNS、メール、GAS、Google Sheetsに触れずに検証できるため。方式Bのテスト用DNS追加は不要になった。
- 外部サービスへの影響：ConoHa、DNS、メール、GAS、Google Sheets、`CNAME` は変更していない。独自ドメイン `sauna-cospa.com` は引き続きConoHa側を表示する。

## GAS v91ソースを安全にローカル保存（2026-09-06・Codex）

- `gomirachelin-work/gas-backup-sanitized/Code.gs` と `mapView.html` を準備した。地図は公開中のv91と同じ、FILTERなし・県境・施設数・施設ピン統合版。
- `gas/README.md` に構成、復元時の設定、公開中GASへ自動反映されないことを記録した。
- 元ソースに直接書かれていたGoogle Sheets ID 2種類はGitHub版から削除した。メインDBはバインド先、全施設統合DBはスクリプトプロパティ `IKITAI_SHEET_ID` から参照する。
- APIキー、WordPress認証情報、楽天API情報の実値がないことを確認した。GAS公開URLのデプロイIDは一般公開済みURLのため残した。
- `Code.gs` はNode.jsの構文検査に合格。公開リポジトリへの送信は、ソース全体を公開する明示承認が必要なため保留。公開中GAS、Sheets、ConoHa、DNSには変更を加えていない。

## ログイン不要の静的サウナ地図をGitHubへ追加（2026-09-06・Codex）

- `map/index.html` を追加し、GASやGoogleログインに依存しないLeaflet＋OpenStreetMapの静的地図にした。
- 実際の都道府県境界47件と、重複を除いた掲載174施設の県別件数を表示する。位置登録54施設は縮尺9以上でピンを表示し、未登録120施設も一覧からGoogleマップ検索を開ける。
- FILTER欄は設けず、都道府県選択、県境クリック、総合点順／コスパ順の切替だけを残した。
- メニューを持つ全60ページの「サウナ地図」を、GAS URLからGitHub内の `map/` へ変更した。各ページから相対パスで開くため、GitHub Pagesのリポジトリ配下URLと将来の独自ドメインの両方で使える。
- 県境データの出典とMITライセンスを `map/MAP-LICENSE.txt` と地図画面内に記載した。
- GitHub mainへコミット `a541566` で反映した。Pages公開URL `https://kenji0618y.github.io/gomirachelin/map/` を未ログインで開き、全国174施設、東京都47施設、47都道府県、地図タイル、一覧を確認した。
- 東京都を選択して縮尺9以上へ拡大すると施設ピンが現れ、スパラクーアのピンから総合点、コスパ、料金、Googleマップ、公式サイトが表示された。
- GAS v91の全員公開は予備として維持する。ConoHa、DNS、GAS本体、Google Sheets、`CNAME` は変更していない。
- 判断と理由：公開メニューはGitHub内の静的地図へ統一する。GoogleログインやGASの公開状態に左右されず、GitHub中心の運用と経費削減の方針に合うため。

## GitHub Pages公開とパス問題の発見（2026-09-06・Claude）

- **Kenjiさんがリポジトリを公開（Public）に変更し、GitHub Pagesを有効化（`main` / `/(root)`）した。** PR #4・PR #5もマージ済み。
- **しかし重大な問題が判明した。** 発行URL `https://kenji0618y.github.io/gomirachelin/` はフォルダ形式のため、このままではサイトが正しく表示されない。
  - サイトは全ページでルート絶対パス（`/why/`、`/css/site.css` など）を使用。実測**1,434か所**。
  - フォルダ形式のURLでは `/css/site.css` が `kenji0618y.github.io/css/site.css`（フォルダの外）を探して404になる。CSS・メニュー・内部リンクがすべて壊れる。
  - 本番 `sauna-cospa.com` ではサイトがドメインのルートにあるため正常に動いている。**サイトの作りの問題ではなく、無料URLがフォルダ形式であることが原因。**
- **方式の選択が必要（Kenjiさんの判断待ち・Codexへ相談中）**：
  - 方式A：リポジトリ名を `kenji0618y.github.io` に変更する（DNS不要、即確認可能、ただしリポジトリ名変更の影響あり）
  - 方式B（Claudeの推奨）：テスト用サブドメイン `test.sauna-cospa.com` を割り当てる（DNSにCNAMEを1行追加。本番とメールには触らない。本番切替の予行演習になる）
- 却下した案：リポジトリ名を `sauna-cospa.com` にする（URLはフォルダ形式のままで解決しない）、1,434か所の相対パス化（リスク大）、`<base>`タグ（`/`始まりには効かない）。
- **詳細と検討根拠は `docs/GitHubPagesのパス問題と方式選択.md` に記載。**
- 判断と理由：方式を決めずに1,434か所を書き換えるのは影響が大きすぎるため、まず公開方式を決める。方式が決まればコード変更はほぼ不要。
- Claudeの環境は `kenji0618y.github.io` にも接続できない（`CONNECT tunnel failed, response 403`）。表示確認・DNS操作・GitHub設定はKenjiさんかCodexが行う必要がある。

## リポジトリ公開に向けた安全確認と伏せ字化（2026-09-06・Claude）

- GitHub Pagesを**無料**で使うにはリポジトリを公開（Public）にする必要があるため、公開して安全かをClaudeが調査した。
- **秘密情報の流出は無し**：リポジトリ全体を走査し、APIキー・パスワード・トークンの類は見つからなかった。`sauna-daigaku.html` のAIチャットは訪問者自身がブラウザ（localStorage）にキーを保存する方式で、コードにキーは埋め込まれていない。
- 一方、`docs/` に非公開の運用情報があったため**伏せ字にした**（計7か所）。以後、実際の値をMarkdownへ書き戻さないこと。
  - `yux6k_（DB名・伏せ字）`、`yux6k_（DBユーザー名・伏せ字）`、`wing-26-（サーバー名・伏せ字）`
  - 実際の値はConoHaのコントロールパネルで確認する。チャットにもMarkdownにも書かない。
- IPアドレス・MX・NS・SPF（`docs/dns-mail-公開記録.md`）はDNSで誰でも調べられる公開情報のため、そのまま残した。
- **決定：リポジトリを公開（Public）にして、GitHub Pagesを無料で使う。**
  - 判断と理由：GitHub Proを契約しても、公開されたPagesサイト自体は誰でも見られる（サイトを非公開にできるのは企業向けプランのみ）。月約600円で隠せるのは `docs/` の中身だけで、それは伏せ字化で無料で解決できるため。
  - サイトのHTML・画像はもともと `sauna-cospa.com` で公開している内容のため、公開リポジトリにしても新たに晒されるものはない。
- **次にKenjiさんへお願いする作業**：①Settings → General → Change repository visibility で Public に変更、②Settings → Pages で `main` / `/(root)` を選んで Save。
- 今後の予定（後回し）：サウナ大学のAIチャットは、Googleの無料APIキーを載せる方式へ変更する予定。現在の方式は当面そのまま。

## サウナ地図の一般公開とメニュー統一（2026-09-06・Claude）

- **公開前ブロッカーが解消した。** KenjiさんがGASの公開設定を「アクセスできるユーザー：全員」へ変更し、シークレットウィンドウ（未ログイン）で地図が開けることを確認した。既存デプロイの編集で行ったためURLは変わっていない。
- これを受けて、**59ページのメインメニューへ「サウナ地図」を追加**した。差分は1ページあたり1行のみ（`<a href="...exec?page=map" target="_blank" rel="noopener">サウナ地図</a>` を「サウナ大学」の直後に挿入）。
  - `index.html` は追加済みだったのでスキップ。
  - `sauna-daigaku.html` はアプリ専用の別メニュー（ホーム／学ぶ／腕試し／用語辞典）のため対象外。
- 確認結果：
  - 地図リンクを持つページ：**60ページ**（サイトのメニューを持つ全ページ）。
  - メニュー構成の一致：**60ページすべてが「トップ／WHY／ランキング／サウナ大学／サウナ地図／お問い合わせ」で完全一致**（従来は `index.html` だけが異なっていた）。
  - ヘッドレスブラウザでPC幅（1280px）の表示崩れ0件を確認。モバイル幅では既存仕様どおりハンバーガーメニュー内に入る（`css/site.css` の860px以下で `.nav-links` を折りたたむ設計）。
- 判断と理由：地図が未ログインで開けることを確認できたため、ロードマップの中止条件（「地図が未ログインの一般訪問者に開けない場合は本番メニューへ地図リンクを追加しない」）が解除されたと判断した。
- ConoHa、GAS本体、Google Sheets、本番サイトは変更していない（GASは公開設定のみKenjiさんが変更）。

## サウナ大学画像移行の検証と地図方式の決定（2026-09-06・Claude）

- Codexが実施したサウナ大学画像のGitHub移行（`146bfa6`、`0f89c3b`）を、Claudeが機械的に検証した。すべて問題なし。
  - `assets/images/sauna-university/` の画像：**35枚**。`sauna-daigaku.html` が参照するパス：**35件**。過不足0件。
  - 全35枚がJPEGとして正常（先頭`FFD8`・末尾`FFD9`を確認）。ローカルサーバー経由で全35件がHTTP 200。
  - `sauna-daigaku.html` に残る `wp-content/uploads` 参照：**0件**。
  - ヘッドレスブラウザでトップの背景写真が正しく表示されることを確認した。
- リポジトリ全体（`docs/` を除く62ファイルのHTML）を再走査した。
  - 自サイトのWordPress固有URL：**0件**。`ランキング/index.html` に残る `?page_id=2` は外部施設サイト（`kamifurano-hokkaido.com`）のURLで、修正不要と判断した。
  - サイト内リンク切れ：**実質0件**。検出された7件はすべて `sauna-daigaku.html` のJavaScriptテンプレート文字列（`${ch.photo}` など）で、実行時に値が入るため誤検出。
- **決定：サウナ地図は、GASの公開設定を「アクセスできるユーザー：全員」に変更する方式で進める。** Kenjiさんが選択した。
  - 判断と理由：GAS地図v91はすでに完成しており、共有設定を変えるだけで一般訪問者が開ける。地図を作り直すより速く、確実で、既存の動作を壊さないため。
  - 掲載内容はもともとサイトで公開している施設情報のみのため、公開範囲を広げる影響は小さいと判断した。
  - この変更にはブラウザ操作が必要なため、Kenjiさんまたはブラウザを操作できるAIが実施する。Claudeからは手順を案内する。
  - 将来ConoHaとGASへの依存を完全に無くしたい場合は、Leaflet＋OpenStreetMapの静的地図をGitHub内に作る案が残っている。その場合は施設の緯度経度データ（現在はGoogle Sheetsにのみ存在）の書き出しが必要。
- Claudeが変更したファイルは無い（検証と記録のみ）。ConoHa、GAS、Google Sheets、本番サイトは未変更。

## ConoHaデータベース保存完了（2026-09-06）

- phpMyAdminから対象DB `yux6k_（DB名・伏せ字）` をQuick / SQLでエクスポートした。
- `gomirachelin-work/backups/conoha/2026-09-05/database/yux6k_（DB名・伏せ字）-2026-09-06.sql` に保存した（4,674,654 bytes）。
- phpMyAdmin SQLヘッダー、CREATE TABLE 18件、INSERT文103件、HTMLエラー混入0件を確認した。SHA-256は同フォルダーの `manifest.csv` に保存した。
- WordPress本体、`wp-content`、画像、設定、現行 `.htaccess`、SQLが揃い、復旧可能なバックアップが成立した。
- 未取得10項目は完全スナップショットとの差として記録を残す。施設5フォルダーはGitHub mainにも静的版がある。残るルート5ファイルは配布文書・旧バックアップ設定で、復旧の必須構成ではない。
- 判断と理由：フェーズ1のバックアップ要件を満たしたものとして次の整理へ進む。公開中のサイトを戻せる主要データが揃い、差分も明示できたため。
- ConoHa本番、DNS、データベース内容、GASは変更していない。

## ConoHaバックアップ実体検証（2026-09-06）

- 分割ZIPを `gomirachelin-work/backups/conoha/2026-09-05/web/` へ保存し、`manifest.csv` にSHA-256を記録した。
- `web/extracted/sauna-cospa.com/` へ統合解凍した。8,450ファイル、189,625,554 bytes、同名内容の衝突0件。
- `wp-content/uploads` は440ファイル、28,451,010 bytes。サウナ大学の元画像 `sauna-univ-01.jpg`〜`45.jpg` も保存済み。
- ConoHaルート88項目のうち78項目を保存。施設5フォルダーとルート5ファイルの計10項目は、取得したZIP内に無かったため追加取得が必要。
- `.htaccess` はWordPress標準の書換えとCocoonのキャッシュ・圧縮設定で、旧URLから新URLへの独自リダイレクトは無い。
- DBエクスポートは未実施。ConoHaのセッションは期限切れ。本番ファイル、DB、DNS、GASは変更していない。
- 判断と理由：Webを「完了」ではなく「主要データ保存済み、残り10項目」と記録する。別のAIが完全バックアップと誤認するのを防ぐため。

## ConoHaバックアップ作業中の利用制限チェックポイント（2026-09-05・Claude）

- 利用者（Kenjiさん）から「そろそろセッション上限」との申告があり、AGENTS.mdルール11に従い新しい作業を始める前に記録を保存した。
- **進行中の作業**：ConoHaバックアップ手順（`docs/ConoHaバックアップ手順.md`）のフェーズ2「phpMyAdminからDBをSQL形式でエクスポート」。Kenjiさんがブラウザを操作し、Claudeが手順を文章で案内する形で進行中。
- **未解決の問題**：ConoHaコントロールパネルの「データベース」画面から「管理ツール」欄の`phpMyAdmin`リンクをクリックしても、`phpmyadmin2003.conoha.ne.jp`への接続が`ERR_CONNECTION_TIMED_OUT`になりアクセスできない。直接URL入力でも、パネル経由のリンクでも同じ結果。原因はConoHa側ではなく、利用者のネット回線・PC側（セキュリティソフトやルーターが該当ドメインをブロックしている等）の可能性が高いと判断した。
  - 提案した切り分け方法（未実施）：①スマートフォンのモバイル回線で同じリンクを試す、②PCのセキュリティソフトを一時確認、③別のブラウザで試す。
  - 代替案（未実施）：phpMyAdminでの手動SQLエクスポートにこだわらず、ConoHaの「自動バックアップ」機能でデータベースを含むサーバー全体のバックアップが取れているかを先に確認する案も提示済み。
- DBユーザー名は`yux6k_（DBユーザー名・伏せ字）`、パスワードはKenjiさんが確認済み（このMarkdownやチャットには記録していない。安全のため）。
- **安全上の注意（引き続き有効）**：ConoHaパネルのDBユーザーのパスワードリセット（鉛筆マーク）は絶対に使わない。本番WordPressのDB接続が壊れるリスクがあるため。wp-config.phpは閲覧のみで保存・上書きしない。パスワードやSQLダンプの中身はチャットに貼らない。
- PR #4（`docs/公開サイトとGitHub差分.md`、未マージ）、PR #5（小さな安全修正、未マージ）は本セッション中に2回状態確認（13:15Z、14:18Z時点）。両方とも`mergeable_state: clean`、レビュー・コメント0件、変更なし。「マージせず止めて」の指示は継続中。60分後（15:21Z）に次の自動チェックインを設定済み。
- **次にやること**：①上記の切り分け方法をKenjiさんに試してもらう、②ダメなら自動バックアップ機能の確認に切り替える、③DBエクスポートが完了したら`docs/ConoHaバックアップ手順.md`の残り（Webデータのファイル取得、DNS・メール記録）に進む。
- 判断と理由: セッション上限が近いとの申告を受け、新しい作業に進む前に途中経過を保存した。次にどのAI・セッションが引き継いでも、phpMyAdminの未解決問題と安全上の注意点がすぐ分かるようにするため。

## 小さな修正とrobots.txt追加（2026-09-05・Claude、Codex利用制限中の引き継ぎ）

- Codexが利用制限に達したため、ConoHaに触れない範囲の安全な修正をClaudeが引き継いだ。ConoHaへのログイン操作はClaude単体ではできない（ブラウザ操作の手段がない）ため、利用者と相談のうえ「私が安全なGitHub作業を先に進める」方針で合意した。
- PR #4（`docs/公開サイトとGitHub差分.md`、未マージ）で見つけたWordPress固有URL2件を修正した。
  - `index.html` の「WHY GOMIRACHELIN？」ボタン：`/?page_id=37` → `/why/`
  - `sauna-daigaku.html` の「正直レビュー一覧」リンク：`https://sauna-cospa.com/category/review/` → `https://sauna-cospa.com/reviews/`
- `reviews` を `data/urls.json` に登録した（`id:0`, `type:page`, 日付は`reviews/index.html`追加コミットの日時）。
- `robots.txt` を新規作成した。中身は `User-agent: * / Allow: / / Sitemap: https://sauna-cospa.com/sitemap.xml` の最小構成。本番の実際の内容はPR #4のB-2でCodex側の確認待ち。本番に別内容がある場合はそちらを優先し、この最小版を上書きする。
- 修正後、サイト内リンク切れ0件、`data/urls.json`とフォルダの整合を再確認。ヘッドレスブラウザでトップページの表示崩れがないことも確認した。
- 変更ファイルは `index.html`、`sauna-daigaku.html`、`data/urls.json`、`robots.txt` の4件のみ。ConoHa、GAS、Google Sheets、本番サイトは未変更。
- 判断と理由: ConoHaバックアップと独立して安全に進められる項目を先に片付けることで、Codex復帰後はバックアップ作業に専念できるようにした。

## 公開サイトとGitHubの差分調査（2026-09-05・Claude）

- ロードマップのフェーズ1「公開サイトとGitHubの差分一覧」をClaudeが担当し、`docs/公開サイトとGitHub差分.md` を新規作成した。
- **作業環境から `https://sauna-cospa.com` へ到達できず**（curl・WebFetchとも `EGRESS_BLOCKED`）、本番側の直接確認はできなかった。GitHub側から確定できる事実と、本番側で確認が必要な項目を分けて記録した。
- **最重要の発見**：`sauna-daigaku.html` がWordPressの `wp-content/uploads/2026/07/` にある写真**35枚**を直接参照している。ConoHa解約でサウナ大学の写真が全て表示されなくなるため、バックアップに `wp-content/uploads/` を必ず含める必要がある。
- リポジトリ内の画像は `sauna-icon-180.png` の**1枚のみ**、PDFは0件であることを実測した。
- GitHub mainに `robots.txt`、`.htaccess`、`favicon.ico`、`ads.txt` が無いことを確認した。本番側の有無は未確認。
- WordPress固有URLの残存2件を特定した。`index.html` の `/?page_id=37`（→ `/why/`）と、`sauna-daigaku.html` の `/category/review/`（→ `/reviews/`）。
- メニューは61ページ中59ページが同一構成で、`index.html` だけ「サウナ地図」を含む。地図は未ログインでは開けないため、揃え方は地図の公開方法決定後に判断する。
- サイト内リンク切れは0件、フォルダ・`data/urls.json`・`sitemap.xml`・`FACILITIES` の整合も確認済み（`reviews` の `urls.json` 未登録1件のみ）。
- 本調査はPRのみ作成しマージしていない。サイトのコード・画像は1ファイルも変更していない。ConoHa、GAS、Google Sheets、本番サイトも未変更。
- 判断と理由: 本番へ到達できない事実を隠さず明記し、確認手順つきの空欄表を用意した。ConoHaにアクセスできる担当が追記すれば調査を完成できるため。

## ConoHaバックアップ準備（2026-09-05）

- ロードマップのフェーズ1「ConoHaの棚卸しとバックアップ」を開始した。
- ConoHa管理画面のログイン期限が切れ、データベース画面へ進む途中でログイン画面へ戻った。再ログイン後に続行する。
- ConoHa公式手順を確認し、自動バックアップは過去14日分であること、Web・Mailのリストア後ファイルは24時間で削除されること、恒久保存にはWebデータの手元取得とphpMyAdminからのDB書き出しが必要なことを確認した。
- ローカルに `gomirachelin-work/backups/conoha/2026-09-05/` を作り、`web`、`database`、`dns-mail` の保存先を準備した。このバックアップ領域は秘密情報を含む可能性があるためGitHubへ入れない。
- `docs/ConoHaバックアップ手順.md` を作成し、全体ロードマップとHTML版の「次の1手」をConoHaバックアップへ更新した。
- ConoHa上のファイル、データベース、DNS、メール、契約設定は変更していない。
- 判断と理由: ConoHaの14日間の自動バックアップだけに頼らず、解約後も復旧できる手元バックアップを先に作る。

## 利用制限前の引き継ぎ強化（2026-09-05）

- 利用者から、突然利用制限に達してもGrok、Claude、Gemini、CodexがMarkdownだけで再開できるようにしてほしいと要望があった。
- `AGENTS.md` に、残り約20%以下、コンテキスト約80%以上、利用制限警告時の強制チェックポイントと、作業中の区切りごとの記録ルールを追加した。
- `AGENTS.md` を全AI共通ルールの唯一の正文にした。ClaudeとGeminiは `AGENTS.md` を直接自動読込しないため、`CLAUDE.md` と `GEMINI.md` には規則を複製せず、公式のimport記法を1行だけ置いた。
- GrokとCursorは `AGENTS.md` を直接読めるため、不要な `GROK.md` と `.cursor/rules/update-project-md.mdc` は削除した。
- 一元化した `AGENTS.md`、1行importの `CLAUDE.md`・`GEMINI.md`、`docs/LIMIT_CHECKPOINT.md`、状態記録をGitHubのmainへ反映した。GitHubに残っていたCursor専用ルールも削除済み。PRは作らず、小さな直接コミットで反映した。
- `docs/LIMIT_CHECKPOINT.md` を新設し、現在の作業、外部サービス状態、次の1手、中止・復旧条件を保存した。
- Codexのリセット後の確認値は、5時間枠2%使用、週間枠0%使用、利用可能なリセットクレジット0件。数値は2026-09-05確認時点で、アカウント全体の使用状況として変動する。
- 判断と理由: セッション終了時だけの記録では突然の利用制限に対応できないため、外部変更直後と制限警告時に先にMarkdownを保存する。
- 判断と理由: AI別ファイルへ同じ規則を複製すると更新漏れで内容がずれるため、今後規則を変更するファイルは `AGENTS.md` だけにする。

## Claudeとの選択統合（2026-09-05）

- Claudeへ最新mainを基点にした読み取り確認を依頼し、`sauna-daigaku.html` のリンク切れ2件は `sauna-app.webmanifest` と `sauna-icon-180.png` の不足だと確認した。
- Claudeが安全確認済み2ファイルだけのPR #3を作成。CodexがGitHubの差分を確認し、mainへマージした。
- PR #3は新規2ファイル、既存ファイル変更0件。除外対象の `docs/STATE.md`、`404.html`、`.nojekyll`、`CNAME`、サイトマップ、`gas/github_post.gs` は含まれていない。
- GitHub側のリンク切れ2件は解消済み。黒×金のGアイコンは代替品で、元のアイコンが見つかれば差し替え可能。
- `404.html` は不要な `/サイトマップ/` 導線を3か所含み、現在のConoHaでは自動適用されないため、GitHub Pages方式決定まで保留する。
- ConoHa本番、DNS、GASの公開設定は変更していない。

## Claude再確認後の検証（2026-09-05）

- 未ログイン状態のアプリ内ブラウザーでGAS地図URLを開くとGoogleログイン画面になり、一般訪問者が現在の地図を開けないことを確認した。
- GitHub mainのトップ `index.html` には地図リンクがあるが、ConoHa本番には未反映。一般公開できる方式が決まるまで本番メニューへ反映しない。
- Claudeブランチ `claude/grok-stopped-tznre4` とmainを比較し、7コミット・85ファイルの変更、自動マージ不可を確認した。
- Claudeブランチには、撤回済みのHTMLサイトマップを全ページへ追加する変更と、独自ドメイン切替前の `CNAME` が含まれる。
- ブランチ全体はマージしない。アイコンとwebmanifestはPR #3で個別に取り込み済み。404ページ、必要性確認後の `.nojekyll`、有用なドキュメントは必要時に個別確認する。
- `gas/github_post.gs` はGitHubへの書き込み権限を使うため、現在のGAS本体と投稿方法を確認してから安全性を別途検証する。
- 判断と理由: Claudeの指摘どおり地図の公開範囲は最優先で直す。一方、利用者が不要としたサイトマップや時期尚早なDNS設定まで混ざるため、ブランチの丸ごと統合は行わない。
- 検証結果と選択統合の方針を、全体ロードマップ、HTML版、AI引き継ぎとともにGitHubのmainへ反映済み。

## GitHub移行ロードマップ（2026-09-05）

- `docs/GitHub移行ロードマップ.md` を「全体ロードマップ」へ拡張し、移行以外のサイト改善ToDoも優先A・B・Cに分けて追加してGitHubのmainへ反映済み。
- 利用者が一目で現在地と次の作業を確認できるよう、操作できるHTML版 `docs/roadmap.html` を作成してGitHubのmainへ反映済み。フェーズ選択とToDoの絞り込みをブラウザーで確認した。
- ローカルの作業管理用 `TASKS.md` にActive、Waiting On、Someday、Doneを整理した。
- `AGENTS.md` に、予定や優先順位が変わったらMarkdown版とHTML版のロードマップを一緒に更新するルールを追加し、GitHubのmainへ反映済み。
- 方針: ConoHaへの日常的な更新を止め、GitHubを唯一の編集元にする。ConoHaは移行完了まで旧サイト兼復旧用として残す。
- 移行は、バックアップ → 不足補完 → GitHub Pagesテスト公開 → 独自ドメイン準備 → 切替と30日監視 → ConoHa終了、の6段階で進める。
- 移行以外の主な予定は、施設データの整合、座標補完、10項目別ランキングの一元化、レビュー導線、サウナ大学整理、スマートフォン・SEO・問い合わせの点検。
- 現在の次の作業は、ConoHaを変更せずに棚卸しとバックアップを行い、公開サイトとGitHubの差分一覧を作ること。
- 判断と理由: ConoHaを先に解約せず、GitHub側の確認後も30日間残す。欠落やDNS問題が見つかった場合に元のサイトへ戻せるため。
- 判断と理由: MarkdownはAIの引き継ぎ用、HTMLとCanvasは利用者の確認用として併用する。一覧性と詳細記録の両方を保つため。

## 地図統合版とAI引き継ぎ（2026-09-05）

- 最新の希望: FILTER欄を削除し、都道府県境界と施設ピンを1枚の地図へ統合する。縮尺を上げると施設ピンが現れ、クリックできるようにする。完成後はサイトのメニューバーへ「サウナ地図」を追加する。
- 統合版 `mapView.unified.html` をローカル作成し、JavaScript構文とブラウザー表示を確認済み。FILTER欄と地図切替タブは削除済み。
- 縮尺9未満では都道府県別の掲載施設数、縮尺9以上では位置情報のある施設ピンを表示する。ピンからGoogleマップと公式サイトを開ける。
- 東京都では掲載47施設のうち位置登録24施設、未登録23施設。未登録施設も下の一覧からGoogleマップ検索を開ける。
- スパ ラクーアのピンをクリックし、施設名、点数、料金、Googleマップ、公式サイトが表示されることを確認した。
- GAS統合版を既存URLのバージョン91として公開完了（2026/09/05 12:58）。公開画面でFILTERなし、地図切替なし、全国174施設、東京都47施設を確認した。
- 公開画面で東京都を縮尺9以上まで拡大し、位置登録24施設、未登録23施設、画面上の施設ピン54個を確認。草加健康センターのピンから施設情報、料金、Googleマップ、公式サイトが開ける表示を確認した。
- GitHubのトップ `index.html` のメインメニューへ「サウナ地図」を追加し、mainへコミット `1f2cad1` で反映済み。ConoHaの公開サイトへのアップロードは未実施のため、`sauna-cospa.com` のメニューにはまだ反映されていない。
- ConoHa管理画面はログアウト状態でログインページが表示された。本番メニュー反映を続けるには、利用者によるConoHaへのログインが必要。
- Claudeの共有セッションを確認し、要点を `docs/AI引き継ぎ.md` に記録した。
- `AGENTS.md`、`.cursor/rules/update-project-md.mdc`、`CLAUDE.md`、`GEMINI.md` に、すべてのAIが毎回Markdownを更新する共通ルールを追加し、GitHubのmainへ反映済み。
- Claudeとの作業内容は `docs/AI引き継ぎ.md` としてGitHubのmainへ保存済み。
- 判断と理由: 県別集計と施設ピンを同じLeaflet地図にまとめ、縮尺で表示を切り替える。県境と全体件数を見やすく保ちながら、拡大時に個別施設を選べるため。
- 判断と理由: AIごとの会話だけに依存せず、`AGENTS.md` とMarkdownへ作業状態を残す。Codex、Claude、Geminiの間で同じ状態から再開できるようにするため。

## GAS地図の修正（2026-09-05）

- 利用者の希望: 都道府県を実際の県境の地図にし、各県の数字を「掲載サウナ施設数」にする。
- GASの mapView.html と コード.gs の地図データ取得部分を修正。既存公開URLのバージョン89から90へ更新完了（2026/09/05 0:17）。GitHubの静的サイトへの反映は未実施。
- 以前の数字は最高評価点だった。修正版は県別の施設数を表示し、県選択・検索・条件変更後の一覧件数と一致させる。
- 正規化は既存処理を使用。「お風呂の国」は公式住所で神奈川県と確認し地図表示だけ補正。元シートは未変更。
- 未評価の施設も含め、同名・同県・同エリアの重複は集計時に1施設とする。異なる支店は維持。
- 現在の登録データでは国内174施設（海外1行を除き、重複1行を集約）。閉店・閉館の記録3施設を除くフィルタでは171施設。
- 東京都47施設、神奈川県7施設、広島県17施設等。数字は日本全国に存在する全サウナの数ではなく、本サイトの登録施設数。
- 全47都道府県の境界、県別件数合計174、東京都の一覧47件、重複・未評価・座標未登録の集計を確認。公開後も全国174施設、広島県17施設と一覧17件の一致を確認し、修正版をアプリ内ブラウザーに表示。
- 判断と理由: 県別集計に施設の緯度経度は不要とする。ピン未登録でも県別施設数に数えるため。
- 判断と理由: 最初は登録施設全件を表示し、閉店記録の除外は明示的な切替にする。フィルタのために数が変わる理由を分かるようにするため。
- 県境は国土数値情報由来の piuccio/open-data-jp-prefectures-geojson を画面用に簡略化。出典リンクとMITライセンスを保存し地図にも表示。
- バックアップと修正版: gomirachelin-work/map-fix/（Code.original.gs、mapView.original.html、Code.gs、mapView.html）。元の公開版はGASのバージョン89。

## 以前の静的サイト作業（ローカルの下書き・GitHubと本番には未反映）

- 最新の希望は、GASにあるサウナ施設の地図ページを表示すること。ページ一覧のサイトマップは不要と訂正された。
- 作成したHTMLサイトマップ、生成・メニュー追加スクリプトを削除。トップと既存の検索エンジン向けXMLは変更前の原本に戻した。
- 決定と理由: HTMLサイトマップの追加を撤回する。利用者が求めているのはGASの地図のため。
- GASの「日本サウナ地図」を確認し、アプリ内ブラウザーで表示。初期表示の都道府県地図・施設一覧170件を確認。公開URL: https://script.google.com/macros/s/AKfycbwmOIUlsf4rA8fw8za5wbtsM-No2UDFDAFQHRhKrqwQ4lUUXAJlXcbpGOQuPhaNK0IH/exec?page=map
- 地図が分かりづらいという希望を受け、表示中のGASページを既存の「ピン地図」に切り替えた。OpenStreetMapの地図・地名・施設ピンを画面で確認。GASソースや既定の表示設定は未変更。
- ピン地図には「119件は座標が未取得のため地図に表示されていません」と表示される。全施設の位置表示には座標の補完と確認が必要。
- ConoHaは利用者によるとあと約半年利用可能。移行の準備と確認を進める。
- 公開ファイルを public/ へまとめる案は準備のみで、フォルダー移動は未実施。
- 69ファイルの一覧を確認済みだが、編集用ソースは一部のみ取得済み。GitHub連携の取得は404、一括ダウンロードはChromeでブロックされ、完全な取得は未完了。
- GitHubへの反映、本番反映、ConoHaの解約・設定変更は未実施。
- GitHubだけへの移行は未決定。詳細は docs/移行準備.md。

---

## このサイトは何か
全国のサウナ施設を独自の10項目で評価し、料金（コスパ）をもとに格付け・紹介する正直ガイドWebサイト「**GOMIRACHELIN（ゴミラシュラン）**」の静的HTMLファイル群です。
WordPress（`https://sauna-cospa.com`）から書き出したもので、ConoHaサーバーに直接アップロードして公開するための構成になっています。

---

## できていること（ページ・機能）

### ページ
- **トップページ (`index.html`)**：キャッチコピー、サイトのコンセプト説明（WHY）、10項目の評価基準、総合トップ3サウナの表示。
- **ランキングページ (`ランキング/index.html`)**：全国176施設のコスパランキング（総合／コスパの切替つき）。記事があるものは施設名から個別レビューへ飛べます。
- **レビュー一覧ページ (`reviews/index.html`)**：記事のあるサウナ47軒を新着順に並べた一覧。**新設（2026-09-04）**。
- **評価基準ページ (`10項目別ランキング/index.html`)**：10項目それぞれの順位一覧（①サウナ〜⑩ホスピタリティ）。
- **WHYページ (`why/index.html`)**：サイトの理念や評価方針の説明。
- **サウナ大学 (`sauna-university/index.html`, `sauna-daigaku.html`)**：サウナに関する解説記事・コンテンツ。
- **各種案内ページ**：`お問い合わせ`、`運営者情報`、`プライバシーポリシー`、`免責事項`。
- **各サウナ施設の個別レビューページ**：50ページ（うち47軒がランキング掲載施設、1本がコラム、2本が同じ施設の重複記事）。

### 機能・デザイン
- **共通スタイル (`css/site.css`)**：黒背景と金色を基調とした共通のヘッダー・ナビゲーション・フッターデザイン。
- **サイトマップ・データ管理 (`sitemap.xml`, `data/urls.json`)**：全ページのURL一覧および記事メタデータ。
- **レビュー一覧の自動生成 (`scripts/gen_reviews.py`)**：`ランキング/index.html` の施設データと `data/urls.json` から `reviews/index.html` を作り直すスクリプト。
- **追加手順のマニュアル (`docs/新規レビュー記事の追加手順.md`)**：新しいサウナのレビューを1軒追加するときの手順書。

### 内部リンクの健全性
サイト内リンクを機械的に確認し、残っていた **`sauna-daigaku.html` の2件もPR #3で解消**しました。GitHub側の既知のリンク切れは0件です。

---

## 次にやること（3つまで）

1. **ConoHaサーバーへのアップロード**
   - 静的ファイル群をConoHaの公開ディレクトリに配置してサイトを公開・更新する。今回の変更ぶんは `index.html` / `sitemap.xml` / `reviews/`（新規フォルダ）。
2. **`reviews/` へのナビゲーション追加を検討**
   - 現在はトップページの「レビュー一覧を見る」ボタンからのみ行けます。上のメニューや各ページのフッターにも足すかどうかを決める（全ページの書きかえになるので要判断）。
3. **`10項目別ランキング` の更新のしやすさ改善**
   - 1軒追加するのに10か所を手で直す必要があるため、`ランキング/index.html` と同じようにデータ1か所から組み立てる形にできないか検討する。

---

## 気になっている点

- `sauna-daigaku.html` と `sauna-university/index.html` という似た役割のファイルが共存している点（Amazonアフィリエイトタグ保持等の理由で別管理されている）。
- ランキングの施設データ（`FACILITIES`）が `ランキング/index.html` 内のJavaScriptコードに直接書かれているため、データ更新時の影響範囲に注意が必要な点。
- `10項目別ランキング/index.html` は10個のリストがすべて手書きのため、1軒追加すると10か所を直す必要がある点。
- `sauna-app.webmanifest` と `sauna-icon-180.png` はPR #3でGitHubへ追加済み。ConoHa本番は未変更のため、本番側の有無はバックアップ時に確認する。
- 施設データの「タイパ」が `0.0` のままの施設がいくつかある（例：神戸 クアハウス、SAUNA BUG）。意図的か未入力かは要確認。
- `reviews/index.html` はWordPressから書き出したページではなく、このリポジトリで作ったページです。WordPressから再書き出しをしても自動では作られないので、`python3 scripts/gen_reviews.py` で作り直してください。

---

## 決定事項と理由

- **`AGENTS.md` と `docs/STATE.md` を新規作成した**
  - **理由**: 作業ルールを統一し、初心者の利用者にもわかりやすくサイトの現状や進捗を管理・共有できるようにするため。
- **トップページの「レビュー一覧を見る」ボタンの飛び先を `/category/review/` から `/reviews/` に変更した**
  - **理由**: `/category/review/` はWordPressの一覧ページで、静的サイト側には書き出されておらず404になっていたため。
- **レビュー一覧ページを `/reviews/`（英字フォルダ）で新規作成した**
  - **理由**: WordPress由来ではない自作ページだと区別しやすく、サーバーへのアップロード時に日本語フォルダ名の文字化けリスクも避けられるため。
- **レビュー一覧は「新着順」にした**
  - **理由**: 点数順は既に `/ランキング/` があるので、役割を分けて「新しく書いたレビューから読める入口」にするため。
- **レビュー一覧を手書きせず `scripts/gen_reviews.py` で自動生成する形にした**
  - **理由**: 施設データを2か所に書くと必ずズレるため、`ランキング/index.html` の `FACILITIES` を唯一の正とするため。
- **同じ施設の重複記事2本（`オアシスサウナ-アスティル-2`、`上野ステーションホステル-オリエンタル2-2`）はレビュー一覧に載せていない**
  - **理由**: 一覧に同じ施設名が2回並ぶのを避けるため。ページ自体は残してあります。
# サイトの現在の状態（STATE.md）

このファイルは、サイトの現在の状況やできていること、次にやることを記録するためのドキュメントです。

最終更新：2026-09-05

## 利用制限前の引き継ぎ強化（2026-09-05）

- 利用者から、突然利用制限に達してもGrok、Claude、Gemini、CodexがMarkdownだけで再開できるようにしてほしいと要望があった。
- `AGENTS.md` に、残り約20%以下、コンテキスト約80%以上、利用制限警告時の強制チェックポイントと、作業中の区切りごとの記録ルールを追加した。
- `AGENTS.md` を全AI共通ルールの唯一の正文にした。ClaudeとGeminiは `AGENTS.md` を直接自動読込しないため、`CLAUDE.md` と `GEMINI.md` には規則を複製せず、公式のimport記法を1行だけ置いた。
- GrokとCursorは `AGENTS.md` を直接読めるため、不要な `GROK.md` と `.cursor/rules/update-project-md.mdc` は削除した。
- 一元化した `AGENTS.md`、1行importの `CLAUDE.md`・`GEMINI.md`、`docs/LIMIT_CHECKPOINT.md`、状態記録をGitHubのmainへ反映した。GitHubに残っていたCursor専用ルールも削除済み。PRは作らず、小さな直接コミットで反映した。
- `docs/LIMIT_CHECKPOINT.md` を新設し、現在の作業、外部サービス状態、次の1手、中止・復旧条件を保存した。
- Codexのリセット後の確認値は、5時間枠2%使用、週間枠0%使用、利用可能なリセットクレジット0件。数値は2026-09-05確認時点で、アカウント全体の使用状況として変動する。
- 判断と理由: セッション終了時だけの記録では突然の利用制限に対応できないため、外部変更直後と制限警告時に先にMarkdownを保存する。
- 判断と理由: AI別ファイルへ同じ規則を複製すると更新漏れで内容がずれるため、今後規則を変更するファイルは `AGENTS.md` だけにする。

## Claudeとの選択統合（2026-09-05）

- Claudeへ最新mainを基点にした読み取り確認を依頼し、`sauna-daigaku.html` のリンク切れ2件は `sauna-app.webmanifest` と `sauna-icon-180.png` の不足だと確認した。
- Claudeが安全確認済み2ファイルだけのPR #3を作成。CodexがGitHubの差分を確認し、mainへマージした。
- PR #3は新規2ファイル、既存ファイル変更0件。除外対象の `docs/STATE.md`、`404.html`、`.nojekyll`、`CNAME`、サイトマップ、`gas/github_post.gs` は含まれていない。
- GitHub側のリンク切れ2件は解消済み。黒×金のGアイコンは代替品で、元のアイコンが見つかれば差し替え可能。
- `404.html` は不要な `/サイトマップ/` 導線を3か所含み、現在のConoHaでは自動適用されないため、GitHub Pages方式決定まで保留する。
- ConoHa本番、DNS、GASの公開設定は変更していない。

## Claude再確認後の検証（2026-09-05）

- 未ログイン状態のアプリ内ブラウザーでGAS地図URLを開くとGoogleログイン画面になり、一般訪問者が現在の地図を開けないことを確認した。
- GitHub mainのトップ `index.html` には地図リンクがあるが、ConoHa本番には未反映。一般公開できる方式が決まるまで本番メニューへ反映しない。
- Claudeブランチ `claude/grok-stopped-tznre4` とmainを比較し、7コミット・85ファイルの変更、自動マージ不可を確認した。
- Claudeブランチには、撤回済みのHTMLサイトマップを全ページへ追加する変更と、独自ドメイン切替前の `CNAME` が含まれる。
- ブランチ全体はマージしない。アイコンとwebmanifestはPR #3で個別に取り込み済み。404ページ、必要性確認後の `.nojekyll`、有用なドキュメントは必要時に個別確認する。
- `gas/github_post.gs` はGitHubへの書き込み権限を使うため、現在のGAS本体と投稿方法を確認してから安全性を別途検証する。
- 判断と理由: Claudeの指摘どおり地図の公開範囲は最優先で直す。一方、利用者が不要としたサイトマップや時期尚早なDNS設定まで混ざるため、ブランチの丸ごと統合は行わない。
- 検証結果と選択統合の方針を、全体ロードマップ、HTML版、AI引き継ぎとともにGitHubのmainへ反映済み。

## GitHub移行ロードマップ（2026-09-05）

- `docs/GitHub移行ロードマップ.md` を「全体ロードマップ」へ拡張し、移行以外のサイト改善ToDoも優先A・B・Cに分けて追加してGitHubのmainへ反映済み。
- 利用者が一目で現在地と次の作業を確認できるよう、操作できるHTML版 `docs/roadmap.html` を作成してGitHubのmainへ反映済み。フェーズ選択とToDoの絞り込みをブラウザーで確認した。
- ローカルの作業管理用 `TASKS.md` にActive、Waiting On、Someday、Doneを整理した。
- `AGENTS.md` に、予定や優先順位が変わったらMarkdown版とHTML版のロードマップを一緒に更新するルールを追加し、GitHubのmainへ反映済み。
- 方針: ConoHaへの日常的な更新を止め、GitHubを唯一の編集元にする。ConoHaは移行完了まで旧サイト兼復旧用として残す。
- 移行は、バックアップ → 不足補完 → GitHub Pagesテスト公開 → 独自ドメイン準備 → 切替と30日監視 → ConoHa終了、の6段階で進める。
- 移行以外の主な予定は、施設データの整合、座標補完、10項目別ランキングの一元化、レビュー導線、サウナ大学整理、スマートフォン・SEO・問い合わせの点検。
- 現在の次の作業は、ConoHaを変更せずに棚卸しとバックアップを行い、公開サイトとGitHubの差分一覧を作ること。
- 判断と理由: ConoHaを先に解約せず、GitHub側の確認後も30日間残す。欠落やDNS問題が見つかった場合に元のサイトへ戻せるため。
- 判断と理由: MarkdownはAIの引き継ぎ用、HTMLとCanvasは利用者の確認用として併用する。一覧性と詳細記録の両方を保つため。

## 地図統合版とAI引き継ぎ（2026-09-05）

- 最新の希望: FILTER欄を削除し、都道府県境界と施設ピンを1枚の地図へ統合する。縮尺を上げると施設ピンが現れ、クリックできるようにする。完成後はサイトのメニューバーへ「サウナ地図」を追加する。
- 統合版 `mapView.unified.html` をローカル作成し、JavaScript構文とブラウザー表示を確認済み。FILTER欄と地図切替タブは削除済み。
- 縮尺9未満では都道府県別の掲載施設数、縮尺9以上では位置情報のある施設ピンを表示する。ピンからGoogleマップと公式サイトを開ける。
- 東京都では掲載47施設のうち位置登録24施設、未登録23施設。未登録施設も下の一覧からGoogleマップ検索を開ける。
- スパ ラクーアのピンをクリックし、施設名、点数、料金、Googleマップ、公式サイトが表示されることを確認した。
- GAS統合版を既存URLのバージョン91として公開完了（2026/09/05 12:58）。公開画面でFILTERなし、地図切替なし、全国174施設、東京都47施設を確認した。
- 公開画面で東京都を縮尺9以上まで拡大し、位置登録24施設、未登録23施設、画面上の施設ピン54個を確認。草加健康センターのピンから施設情報、料金、Googleマップ、公式サイトが開ける表示を確認した。
- GitHubのトップ `index.html` のメインメニューへ「サウナ地図」を追加し、mainへコミット `1f2cad1` で反映済み。ConoHaの公開サイトへのアップロードは未実施のため、`sauna-cospa.com` のメニューにはまだ反映されていない。
- ConoHa管理画面はログアウト状態でログインページが表示された。本番メニュー反映を続けるには、利用者によるConoHaへのログインが必要。
- Claudeの共有セッションを確認し、要点を `docs/AI引き継ぎ.md` に記録した。
- `AGENTS.md`、`.cursor/rules/update-project-md.mdc`、`CLAUDE.md`、`GEMINI.md` に、すべてのAIが毎回Markdownを更新する共通ルールを追加し、GitHubのmainへ反映済み。
- Claudeとの作業内容は `docs/AI引き継ぎ.md` としてGitHubのmainへ保存済み。
- 判断と理由: 県別集計と施設ピンを同じLeaflet地図にまとめ、縮尺で表示を切り替える。県境と全体件数を見やすく保ちながら、拡大時に個別施設を選べるため。
- 判断と理由: AIごとの会話だけに依存せず、`AGENTS.md` とMarkdownへ作業状態を残す。Codex、Claude、Geminiの間で同じ状態から再開できるようにするため。

## GAS地図の修正（2026-09-05）

- 利用者の希望: 都道府県を実際の県境の地図にし、各県の数字を「掲載サウナ施設数」にする。
- GASの mapView.html と コード.gs の地図データ取得部分を修正。既存公開URLのバージョン89から90へ更新完了（2026/09/05 0:17）。GitHubの静的サイトへの反映は未実施。
- 以前の数字は最高評価点だった。修正版は県別の施設数を表示し、県選択・検索・条件変更後の一覧件数と一致させる。
- 正規化は既存処理を使用。「お風呂の国」は公式住所で神奈川県と確認し地図表示だけ補正。元シートは未変更。
- 未評価の施設も含め、同名・同県・同エリアの重複は集計時に1施設とする。異なる支店は維持。
- 現在の登録データでは国内174施設（海外1行を除き、重複1行を集約）。閉店・閉館の記録3施設を除くフィルタでは171施設。
- 東京都47施設、神奈川県7施設、広島県17施設等。数字は日本全国に存在する全サウナの数ではなく、本サイトの登録施設数。
- 全47都道府県の境界、県別件数合計174、東京都の一覧47件、重複・未評価・座標未登録の集計を確認。公開後も全国174施設、広島県17施設と一覧17件の一致を確認し、修正版をアプリ内ブラウザーに表示。
- 判断と理由: 県別集計に施設の緯度経度は不要とする。ピン未登録でも県別施設数に数えるため。
- 判断と理由: 最初は登録施設全件を表示し、閉店記録の除外は明示的な切替にする。フィルタのために数が変わる理由を分かるようにするため。
- 県境は国土数値情報由来の piuccio/open-data-jp-prefectures-geojson を画面用に簡略化。出典リンクとMITライセンスを保存し地図にも表示。
- バックアップと修正版: gomirachelin-work/map-fix/（Code.original.gs、mapView.original.html、Code.gs、mapView.html）。元の公開版はGASのバージョン89。

## 以前の静的サイト作業（ローカルの下書き・GitHubと本番には未反映）

- 最新の希望は、GASにあるサウナ施設の地図ページを表示すること。ページ一覧のサイトマップは不要と訂正された。
- 作成したHTMLサイトマップ、生成・メニュー追加スクリプトを削除。トップと既存の検索エンジン向けXMLは変更前の原本に戻した。
- 決定と理由: HTMLサイトマップの追加を撤回する。利用者が求めているのはGASの地図のため。
- GASの「日本サウナ地図」を確認し、アプリ内ブラウザーで表示。初期表示の都道府県地図・施設一覧170件を確認。公開URL: https://script.google.com/macros/s/AKfycbwmOIUlsf4rA8fw8za5wbtsM-No2UDFDAFQHRhKrqwQ4lUUXAJlXcbpGOQuPhaNK0IH/exec?page=map
- 地図が分かりづらいという希望を受け、表示中のGASページを既存の「ピン地図」に切り替えた。OpenStreetMapの地図・地名・施設ピンを画面で確認。GASソースや既定の表示設定は未変更。
- ピン地図には「119件は座標が未取得のため地図に表示されていません」と表示される。全施設の位置表示には座標の補完と確認が必要。
- ConoHaは利用者によるとあと約半年利用可能。移行の準備と確認を進める。
- 公開ファイルを public/ へまとめる案は準備のみで、フォルダー移動は未実施。
- 69ファイルの一覧を確認済みだが、編集用ソースは一部のみ取得済み。GitHub連携の取得は404、一括ダウンロードはChromeでブロックされ、完全な取得は未完了。
- GitHubへの反映、本番反映、ConoHaの解約・設定変更は未実施。
- GitHubだけへの移行は未決定。詳細は docs/移行準備.md。

---

## このサイトは何か
全国のサウナ施設を独自の10項目で評価し、料金（コスパ）をもとに格付け・紹介する正直ガイドWebサイト「**GOMIRACHELIN（ゴミラシュラン）**」の静的HTMLファイル群です。
WordPress（`https://sauna-cospa.com`）から書き出したもので、ConoHaサーバーに直接アップロードして公開するための構成になっています。

---

## できていること（ページ・機能）

### ページ
- **トップページ (`index.html`)**：キャッチコピー、サイトのコンセプト説明（WHY）、10項目の評価基準、総合トップ3サウナの表示。
- **ランキングページ (`ランキング/index.html`)**：全国176施設のコスパランキング（総合／コスパの切替つき）。記事があるものは施設名から個別レビューへ飛べます。
- **レビュー一覧ページ (`reviews/index.html`)**：記事のあるサウナ47軒を新着順に並べた一覧。**新設（2026-09-04）**。
- **評価基準ページ (`10項目別ランキング/index.html`)**：10項目それぞれの順位一覧（①サウナ〜⑩ホスピタリティ）。
- **WHYページ (`why/index.html`)**：サイトの理念や評価方針の説明。
- **サウナ大学 (`sauna-university/index.html`, `sauna-daigaku.html`)**：サウナに関する解説記事・コンテンツ。
- **各種案内ページ**：`お問い合わせ`、`運営者情報`、`プライバシーポリシー`、`免責事項`。
- **各サウナ施設の個別レビューページ**：50ページ（うち47軒がランキング掲載施設、1本がコラム、2本が同じ施設の重複記事）。

### 機能・デザイン
- **共通スタイル (`css/site.css`)**：黒背景と金色を基調とした共通のヘッダー・ナビゲーション・フッターデザイン。
- **サイトマップ・データ管理 (`sitemap.xml`, `data/urls.json`)**：全ページのURL一覧および記事メタデータ。
- **レビュー一覧の自動生成 (`scripts/gen_reviews.py`)**：`ランキング/index.html` の施設データと `data/urls.json` から `reviews/index.html` を作り直すスクリプト。
- **追加手順のマニュアル (`docs/新規レビュー記事の追加手順.md`)**：新しいサウナのレビューを1軒追加するときの手順書。

### 内部リンクの健全性
サイト内リンクを機械的に確認し、残っていた **`sauna-daigaku.html` の2件もPR #3で解消**しました。GitHub側の既知のリンク切れは0件です。

---

## 次にやること（3つまで）

1. **ConoHaサーバーへのアップロード**
   - 静的ファイル群をConoHaの公開ディレクトリに配置してサイトを公開・更新する。今回の変更ぶんは `index.html` / `sitemap.xml` / `reviews/`（新規フォルダ）。
2. **`reviews/` へのナビゲーション追加を検討**
   - 現在はトップページの「レビュー一覧を見る」ボタンからのみ行けます。上のメニューや各ページのフッターにも足すかどうかを決める（全ページの書きかえになるので要判断）。
3. **`10項目別ランキング` の更新のしやすさ改善**
   - 1軒追加するのに10か所を手で直す必要があるため、`ランキング/index.html` と同じようにデータ1か所から組み立てる形にできないか検討する。

---

## 気になっている点

- `sauna-daigaku.html` と `sauna-university/index.html` という似た役割のファイルが共存している点（Amazonアフィリエイトタグ保持等の理由で別管理されている）。
- ランキングの施設データ（`FACILITIES`）が `ランキング/index.html` 内のJavaScriptコードに直接書かれているため、データ更新時の影響範囲に注意が必要な点。
- `10項目別ランキング/index.html` は10個のリストがすべて手書きのため、1軒追加すると10か所を直す必要がある点。
- `sauna-app.webmanifest` と `sauna-icon-180.png` はPR #3でGitHubへ追加済み。ConoHa本番は未変更のため、本番側の有無はバックアップ時に確認する。
- 施設データの「タイパ」が `0.0` のままの施設がいくつかある（例：神戸 クアハウス、SAUNA BUG）。意図的か未入力かは要確認。
- `reviews/index.html` はWordPressから書き出したページではなく、このリポジトリで作ったページです。WordPressから再書き出しをしても自動では作られないので、`python3 scripts/gen_reviews.py` で作り直してください。

---

## 決定事項と理由

- **`AGENTS.md` と `docs/STATE.md` を新規作成した**
  - **理由**: 作業ルールを統一し、初心者の利用者にもわかりやすくサイトの現状や進捗を管理・共有できるようにするため。
- **トップページの「レビュー一覧を見る」ボタンの飛び先を `/category/review/` から `/reviews/` に変更した**
  - **理由**: `/category/review/` はWordPressの一覧ページで、静的サイト側には書き出されておらず404になっていたため。
- **レビュー一覧ページを `/reviews/`（英字フォルダ）で新規作成した**
  - **理由**: WordPress由来ではない自作ページだと区別しやすく、サーバーへのアップロード時に日本語フォルダ名の文字化けリスクも避けられるため。
- **レビュー一覧は「新着順」にした**
  - **理由**: 点数順は既に `/ランキング/` があるので、役割を分けて「新しく書いたレビューから読める入口」にするため。
- **レビュー一覧を手書きせず `scripts/gen_reviews.py` で自動生成する形にした**
  - **理由**: 施設データを2か所に書くと必ずズレるため、`ランキング/index.html` の `FACILITIES` を唯一の正とするため。
- **同じ施設の重複記事2本（`オアシスサウナ-アスティル-2`、`上野ステーションホステル-オリエンタル2-2`）はレビュー一覧に載せていない**
  - **理由**: 一覧に同じ施設名が2回並ぶのを避けるため。ページ自体は残してあります。

## フェーズ2の不足整理を開始（2026-09-06 Codex）

- Grokが作成した `docs/公開サイトとGitHubの差分.md` とGitHub mainの構成を照合した。
- `docs/フェーズ2_補完整理計画.md` を作成し、作業を「先に追加・修正」「方式決定後」「公開切替まで保留」に分類した。
- 次の変更は `.nojekyll`、`404.html`、`robots.txt`、トップのWHYリンク修正を小さな単位で準備する。
- `CNAME`、DNS、ConoHa、フォルダー削除は変更していない。
- 判断理由：復旧用バックアップが完成したため、公開先を変えずにGitHub Pagesテストの準備を進められる。

## フェーズ2の基本ファイル追加（2026-09-06 12:30 Codex）

- GitHub mainへ `docs/フェーズ2_補完整理計画.md`、`.nojekyll`、`robots.txt`、`404.html` を小分けで追加した。
- トップページの大きな「WHY GOMIRACHELIN？」ボタンを、WordPress固有の `/?page_id=37` から静的ページ `/why/` へ修正した。
- WHYリンクのコミット `104896e` は、`index.html` の1行だけが変更されたことをGitHub差分画面で確認した。
- GitHub側の関連コミット：計画書 `9419faa`、`.nojekyll` `18891a2`、`robots.txt` `72a655c`、WHYリンク `104896e`。`404.html` もmainのルートに追加済み。
- `CNAME`、DNS、ConoHa、GAS、Google Sheetsは変更していない。GitHub Pagesの独自ドメイン切替も未実施。
- 次はサウナ大学が参照するConoHa画像35件をGitHub内画像へ移す準備と、GAS地図を一般訪問者が開ける方式の決定を進める。
- 判断理由：公開先を変えずに戻せる小さな変更だけを先行し、GitHubテスト公開に必要な土台を安全に整えるため。

## サウナ大学画像の移行準備（2026-09-06 12:45 Codex）

- ConoHaバックアップから、`sauna-daigaku.html` が実際に参照するJPEG 35枚を `assets/images/sauna-university/` へコピーした。合計8,127,222 bytes。
- 35枚すべて画像として読み込めることを確認し、`manifest.csv` にファイル名・容量・SHA-256を保存した。
- ローカルの `sauna-daigaku.html` は旧ConoHa URL 35件をGitHub内パスへ置換済み。旧URLの残りは0件。
- 画像とHTML修正版はGitHubへ未反映。Chrome拡張機能のファイルURL権限が無効で、GitHubの複数ファイル選択が拒否されたため。
- 次の1手：Chrome拡張機能の「ファイルのURLへのアクセスを許可する」をオンにし、画像35枚を先にGitHubへアップロードする。存在確認後にHTMLを反映する。
- 詳細は `docs/サウナ大学画像移行.md`。ConoHa、DNS、GAS、Sheetsは変更していない。

## サウナ大学画像のGitHub移行完了（2026-09-06 12:55 Codex）

- GitHub main の `assets/images/sauna-university/` へ、検証済みJPEG 35枚と `manifest.csv` を追加した（コミット `146bfa6`）。
- GitHubのフォルダー画面で対象35枚と `manifest.csv` がすべて存在することを確認した。
- `sauna-daigaku.html` の旧ConoHa画像URL 35件をGitHub内パスへ変更した（コミット `0f89c3b`）。
- HTMLのコミット差分は `sauna-daigaku.html` 1ファイルだけ、35行追加・35行削除であることを確認した。
- GitHub側では画像とHTMLの移行が完了した。現在公開中のConoHa、DNS、GAS、Google Sheets、`CNAME` は変更していない。
- 次の1手：一般訪問者がGoogleログインなしで使える地図方式を決定し、その後GitHub Pagesのテスト公開へ進む。
- 判断理由：画像を先に配置・確認してからHTMLを切り替える順序にし、参照先の画像切れを防ぐため。
- 戻し方：問題があれば `0f89c3b` を戻すとHTMLだけ旧ConoHa画像URLへ戻せる。画像追加 `146bfa6` は表示に影響しないため、そのまま保管してもよい。
